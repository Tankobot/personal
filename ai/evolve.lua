--Not a serious genetic algorithm 

local args = {...}
local popLim = args[1]
local genLim = args[2]
local targets = {unpack(args)}

local population = {}

for i=1, popLim do
	population[i] = {}
	for j=1, #targets do
		
	end
end

local function crossover(a, b)
	
end